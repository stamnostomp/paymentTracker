export declare type RGB = [string, string, string];
export declare type Color = {
    mode: string;
    color: RGB;
    alpha?: string;
};
export declare function parseColor(value: string, { loose }?: {
    loose?: boolean;
}): null | Color;
export declare function formatColor({ mode, color, alpha }: Color): string;
export declare function colorDetectionRegex({ mode, color, alpha }: Color): RegExp;
